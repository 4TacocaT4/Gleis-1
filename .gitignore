# Abhängigkeiten
node_modules/

# Build-Ausgaben
.next/
out/
*.tsbuildinfo

# Umgebung
.env
.env.local
.env*.local

# Betriebssystem / Editor
.DS_Store
Thumbs.db
# Quellfotos der Menütafel — Arbeitsmaterial, keine Seiten-Assets.
# Der führende Schrägstrich ist wichtig: Ohne ihn schluckt die Regel unter
# Windows (core.ignorecase=true) auch public/images/ und damit das Kartenbild.
/Images/
